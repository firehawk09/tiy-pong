<p><%= notice %></p>
<% if flash[:show_login_form] %>
  <%= render "login" %>
<% end %>
<h1>TIY Houston Table Tennis League</h1>

<% @players.each do |player| %>
  <p><%= player.name %> <%= player.login %>   <%= link_to "Edit", edit_player_path(player) %></p>
<% end %>

<%= link_to  "Join League!", new_player_path %>